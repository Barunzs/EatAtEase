<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/subcategory_card"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="5dp"
    android:clickable="true"
    android:focusable="true"
    app:cardBackgroundColor="@color/stitch_surface_container_lowest"
    app:cardCornerRadius="20dp"
    app:cardElevation="0dp"
    app:strokeColor="@color/stitch_surface_container_high"
    app:strokeWidth="1dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <!-- ── Coloured header block ── -->
        <FrameLayout
            android:id="@+id/card_header_bg"
            android:layout_width="match_parent"
            android:layout_height="120dp">

            <!-- Large emoji centred in the coloured block -->
            <com.google.android.material.textview.MaterialTextView
                android:id="@+id/subcategory_emoji"
                android:layout_width="72dp"
                android:layout_height="72dp"
                android:layout_gravity="center"
                android:background="@drawable/bg_subcategory_emoji_pill"
                android:gravity="center"
                android:textSize="34sp"
                tools:text="🍳" />

        </FrameLayout>

        <!-- ── Name + category indicator ── -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingHorizontal="10dp"
            android:paddingTop="12dp"
            android:paddingBottom="16dp">

            <com.google.android.material.textview.MaterialTextView
                android:id="@+id/subcategory_name"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:ellipsize="end"
                android:gravity="center"
                android:maxLines="2"
                android:textColor="@color/stitch_on_surface"
                android:textSize="13sp"
                android:textStyle="bold"
                android:lineSpacingMultiplier="1.15"
                tools:text="Fresh From Kitchen" />

        </LinearLayout>

    </LinearLayout>

</com.google.android.material.card.MaterialCardView>
